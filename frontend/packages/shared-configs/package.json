{
  "name": "shared-configs",
  "version": "1.0.0",
  "private": true,
  "main": "./index.js",
  "scripts": {
    "build": "echo 'No build step required for configs'",
    "lint": "echo 'Linting shared configuration files'"
  }
}